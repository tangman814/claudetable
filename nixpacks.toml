[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "./node_modules/.bin/tsc --project backend/tsconfig.json",
  "./node_modules/.bin/vite build --config frontend/vite.config.ts"
]

[start]
cmd = "node backend/dist/index.js"
